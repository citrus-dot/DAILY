# DAILY · Technical Documentation

> 单文件 HTML 的「每日任务打卡 + 异步待办」个人工作台。
> 零外部依赖、数据存浏览器本地、可随手导出备份。
> 使用与构建向导见 [onboarding_guide.md](onboarding_guide.md)。

---

## 1. 项目概述

「DAILY」面向需要**自我管理每日目标**的工作者：把任务按类别拆分、给每类设一个可量化的达标门槛，每天在首页打卡，并随时记录待办。

设计目标：

- **轻量**：一个 `.html` 文件，双击即开，手机/电脑通用。
- **可自定义**：任务类别、达标门槛、计量单位全部自定义；内置语义化推荐模板，新建时不用从零想。
- **可视化**：进度环、日历热力图、达标进度条，状态一眼可见。
- **本地优先**：所有数据存浏览器 `localStorage`，不上云；提供导出/导入做备份与迁移。

## 2. 功能模块

| 模块 | 说明 |
| --- | --- |
| **今日概览（顶部横幅）** | 整体达标进度环 + 按时段极简问候与唯美短句；卡片只显示**已钉选**的打卡项快捷「+1」芯片，右上角「+」按钮浮窗勾选（`STATE.pinned`，新建自动加入、删除自动移除）。 |
| **任务类别** | 自定义名称/图标/主题色/计量单位/每日门槛/打卡步长；内置 12 个语义化推荐模板（选图标即自动推荐门槛）。可开启「**详细计划**」按步骤勾选完成，进一步「**分化**」展开周一至周日按钮建立每日专属计划。 |
| **今日打卡** | 每类一张卡：进度环 + 类别图标 + `已累计 / 目标`；达标自动盖章「已达标」；「+步长」弹性滑动累加 + 卡片回弹，「重置」清零（带撤销）。开启「详细计划」的类别显示「今日已推进 X / N 项」，按钮为「下一步」，点卡片弹出计划详情浮窗逐行勾选（空心圈→✔ 绿填充、划去淡化），与首页实时双向同步，勾完即达标。 |
| **统计** | 日历热力图（按当天达标比例上色「未打卡/部分/多数/全部达标」+ 每类达标天数进度条，可翻月）+ **年度热力图**（近 365 天网格）。 |
| **趋势与周报** | 近 30 天 + 近 7 天达标率 SVG 折线（0/50/100% 网格刻度 + 最高/最低/平均数据）+「本周掠影」卡（打卡项次/达标天数/最活跃星期/最长连续天数）。 |
| **待办（异步支线）** | 不随日期刷新的长期小事：手动添加、星标置顶、完成动画删除（横斩/升腾/印章三选一）、纵向拖拽重排、左滑完成/右滑删除、关键词搜索、按语义图标分组、批量管理。 |
| **深色模式** | 浅色/深色/跟随系统三档（偏好存独立键 `wb_theme_pref`）；深色采用**暖炭护眼**配色（页面 `#1b2230`、控件 `#2b313a`、文字 `#cdd3da`），全程无纯白。 |
| **键盘快捷键** | `/` 聚焦待办搜索；`Esc` 关闭弹窗/菜单；输入框聚焦时不误触。 |
| **数据管理** | 导出 **JSON 备份 / CSV 报表 / Markdown 周报** 三种（导入不限条数）、清空示例、清空全部（二次确认）。导出统一走 `deliverExport()`：成功 toast + 失败自动打开手动保存面板（复制/重下）；导入端剥离 BOM/容忍尾部垃圾，失败分「空文件/JSON 不完整/非 DAILY 备份」三类提示。JSON 附 `meta` 元信息，CSV 含达标率列与汇总块，Markdown 含近 4 周趋势与类别排行。安卓端走 SAF 系统保存框，Mac 浏览器降级默认下载。 |
| **视觉与体验** | 设置菜单统一开关：中文字体（LXGW 文楷，base64 内嵌离线可用）、氛围背景（光晕+噪点）、主色随时间流转（按小时切换）、顶栏玻璃质感。 |
| **全达标庆祝** | 今日所有类别达标瞬间：80 片彩屑中心爆发 + 印章「今日圆满 ✦」+ 随机鼓励文案 + 号角音效（可开）；当日仅触发一次（`celebratedDate` 持久化）。 |
| **成就徽章** | 顶栏「勋章」浮窗：已解锁 X / 6 枚（首战告捷/一周不辍/月度坚持/百日长征/百次打卡/年度全勤），未解锁灰显附条件。 |
| **解压音效** | Web Audio 实时合成零素材：滴答/斩击/号角/印章等；默认关闭，设置开启后播放。 |
| **迷你悬浮进度条** | 常驻右下角迷你进度环（`#miniWidget`）实时显示今日 done/total，副文随进度变化；点击跳回今日；今日卡片长按 450ms 快速 +1。 |

## 3. 待办图标语义化系统

待办前置图标为 13 个简约线性 SVG，按文字语义自动分配（`pickTodoIcon`）；不匹配时回退「感叹号」中性样式。

| 图标 key | 颜色 | 触发关键词（含部分） |
| --- | --- | --- |
| `book` 书 | 蓝 | 书/读/课/学/习/文献/paper/study/read |
| `dumbbell` 健身 | 绿 | 健身/运动/锻炼/跑步/瑜伽/力量/训练/球/泳/跑 |
| `flask` 科研 | 紫 | 科研/实验/研究/论文/数据/算法/code/代码/项目 |
| `pen` 写作 | 橙 | 写/作文/文档/报告/笔记/总结/blog/稿 |
| `bulb` 灵感 | 黄 | 灵感/想法/创意/点子/思考/idea/策划 |
| `cart` 购物 | 粉 | 买/购物/采购/超市/囤/商城/下单 |
| `phone` 联系 | 青 | 联系/电话/沟通/微信/邮件/call/email |
| `heart` 健康 | 红 | 健康/医院/体检/看病/牙/药/身体/医 |
| `plane` 出行 | 青绿 | 旅行/出行/机票/出差/回家/飞机/trip/travel/订 |
| `coin` 财务 | 棕 | 钱/缴费/账单/财务/报销/工资/费/卡/税 |
| `bell` 提醒 | 紫蓝 | 提醒/记得/别忘/deadline/截止/到期 |
| `lightning` 加急 | 琥珀 | 急/马上/立刻/urgent/尽快/加急 |
| `exclamation` 默认 | 灰 | 以上均不匹配时的兜底 |

分配逻辑：`addSide()` 新建时 `pickTodoIcon(text)` 存 `icon`；渲染时 `s.icon || pickTodoIcon(s.text)` 兼容旧数据；用户手动改过类型的待办（`#todoTypePicker`）固定不随文案变动。

## 4. 技术架构

- **形态**：单文件 HTML，CSS/JS 全内联，图标全部手写内联 SVG，**零外部请求、零 emoji 图标**；中文字体（LXGW 文楷）base64 内嵌。
- **状态存储**：`localStorage` 键 `wb_daily_state`（**铁律：键名绝不改，只 ADD 字段**），结构：
  ```
  { tasks:[...], records:{ 'YYYY-MM-DD':{taskId:数值} }, sidequests:[...], settings:{...},
    pinned:[...], planDone:{...}, celebratedDate:'', seeded:bool }
  ```
  - `tasks`：`{id,name,icon,color,unit,target,step, plan?:{enabled,diff,steps,days:{0..6:{on,steps}}}}`
  - `records`：按日期存当日累计，每天独立、不跨天滚动（避免虚高达标率）
  - `sidequests`：`{id,text,icon,createdAt,star?,demo?}`
  - 读取时对缺失字段做兜底，旧数据始终兼容。
- **视图切换**：`.view[data-view=today|cats|month|side]`，`showView(v)` 控制显隐 + Tab 高亮 + 滚动置顶；底部 Tab 全尺寸固定。
- **事件与安全**：`#app` 事件委托；用户内容 `esc()` 转义 + 导入 `sanitizeState` 双防御；`save()` try/catch 容错；30s 定时器跨天自动刷新。
- **动画**：进度环弹性滑动、待办横斩（FLIP transform 收拢 + 相邻项弹性回弹）、全达标庆祝（rAF 物理积分粒子）、印章/升腾完成动画；尊重 `prefers-reduced-motion`。
- **音效**：Web Audio `AudioContext` 实时合成（`beep/sweep/bpSweep/noiseBurst/jit` 辅助），零素材。
- **安卓封装**：Capacitor 桥接——`IS_NATIVE` 判断；`capLoad()` 动态加载离线插件运行时；`capApplyReminder()` 本地每日通知（含剩余比例与时段鼓励语、点击跳转）；`capDownloadOrShare()`/`capSaveTo()` 导出（SAF 系统保存框）；`capOnSharedFile` 分享接收导入。浏览器端全部静默降级。**release 签名已配置**（keystore + 密码存 `~/.gradle/gradle.properties`，`bash build-android.sh release` 出正式分发包）。详见 [android-app/README.md](android-app/README.md)。

## 5. 使用说明

1. **新建任务类别**：点「新建类别」→ 选图标（自动推荐门槛）/ 选推荐模板 → 设名称、单位、达标门槛、步长 → 保存。
2. **每日打卡**：今日页点「+步长」累加（进度环弹性滑动 + 脉冲）；「重置」一键清零（可撤销）；达标自动盖章。开启「详细计划」的类别按「下一步」逐行勾选。
3. **添加待办**：待办页输入回车添加，按语义自动分配图标；完成动画三选一（横斩/升腾/印章）；支持搜索、分组、批量、拖拽重排、滑动操作。
4. **外观与快捷键**：主题切换浅色/深色/跟随系统；`/` 聚焦待办搜索、`Esc` 关弹窗。
5. **设置（齿轮菜单）**：中文字体、氛围背景、主色流转、解压音效、完成动画、悬浮进度条、每日提醒（仅手机 App 生效）。
6. **备份与迁移**：数据管理 → 导出 JSON/CSV/Markdown；换设备前导出、新环境导入。

## 6. 设计原则

- 图标一律内联 SVG，禁用 emoji 当图标。
- `localStorage` 键名 `wb_daily_state` 绝不改，只允许 ADD 字段（旧数据必须能读出）。
- 导出/导入不限条数；「清空全部数据」需二次确认。
- 预置示例数据含「今日未达标」项，便于一进来就看到待处理与动画效果。
- 数据闭环：seed / load / import / clearSeed / clearAll 全部兼容各字段。

## 7. 文件清单

| 文件 | 作用 |
| --- | --- |
| `DAILY.html` | 主程序（单文件应用） |
| `README.md` | 项目简介 |
| `onboarding_guide.md` | 使用与构建总向导 |
| `Technical-Documentation.md` | 本文档 |
| `Android-Packaging.md` | 移动端封装方案与选型记录 |
| `docs/specs/` | 功能设计文档 |
| `android-app/` | Capacitor 安卓壳工程 |

## 8. 已知限制 / 后续可扩展

**限制**

- 数据仅存单浏览器 `localStorage`，换设备需手动导出/导入（无云端同步）。
- 待办图标为前端关键词匹配，极长/抽象文本可能命中默认感叹号。
- 安卓 WebView 的 `localStorage` 在「清缓存/卸载应用」时会丢，需依赖导出备份。

**可扩展方向**

- 连续未达标天数统计、待办累计完成次数统计、多人/多设备/云同步、待办完成历史（todoLog）、周报趋势折线内联化。
